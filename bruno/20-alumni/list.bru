meta {
  name: Search Alumni Directory
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/alumni/directory
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
}

assert {
  res.status: eq 200
}
