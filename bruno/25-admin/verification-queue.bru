meta {
  name: Verification queue
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/admin/verification/queue
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}

assert {
  res.status: eq 200
}
