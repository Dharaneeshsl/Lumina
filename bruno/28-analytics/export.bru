meta {
  name: Export analytics data
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/analytics/export
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}

assert {
  res.status: eq 200
}
