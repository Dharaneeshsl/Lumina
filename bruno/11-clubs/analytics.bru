meta {
  name: Get club analytics
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/clubs/{{clubId}}/analytics
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
