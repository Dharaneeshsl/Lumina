meta {
  name: Leave club
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/clubs/{{clubId}}/leave
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}

assert {
  res.status: eq 200
}
