meta {
  name: List club members
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/clubs/{{clubId}}/members
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}

assert {
  res.status: eq 200
}
