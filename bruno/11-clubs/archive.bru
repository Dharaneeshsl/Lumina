meta {
  name: Archive club
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/clubs/{{clubId}}/archive
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}

assert {
  res.status: eq 200
}
